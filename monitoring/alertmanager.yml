global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'
  routes:
    - match:
        severity: critical
      receiver: 'critical'
      continue: true
    - match:
        severity: warning
      receiver: 'warning'
      continue: true

receivers:
  - name: 'default'
    # デフォルトはログのみ

  - name: 'critical'
    webhook_configs:
      - url: 'http://host.docker.internal:5001/alert/critical'
        send_resolved: true

  - name: 'warning'
    webhook_configs:
      - url: 'http://host.docker.internal:5001/alert/warning'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']
